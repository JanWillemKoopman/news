/** Tailwind CSS v4 draait volledig als PostCSS-plugin; geen tailwind.config.js meer —
 *  het thema staat in app/globals.css onder @theme. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
