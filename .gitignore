# Claude Code internal files (settings.json + hooks are committed on purpose
# so they apply to every session; only local/machine-specific state is ignored)
.claude/settings.local.json
.claude/hooks/.npm-install-stamp

# Dependencies
node_modules/
/.pnp
.pnp.js

# Next.js
/.next/
/out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build
/build

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts
